AI Fitness Planner - React


Instructions:
1. Create a React app folder and copy the files accordingly.
2. Install dependencies: npm install
3. Start: npm start


Notes:
- Configure API_BASE_URL in src/config.js (defaults to http://localhost:8000)
- Tailwind is optional; styles use plain CSS included in src/index.css. If you prefer Tailwind, replace styles.
